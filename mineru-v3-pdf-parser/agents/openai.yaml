interface:
  display_name: "Parse PDFs with MinerU v3"
  short_description: "Parse PDFs and bundle Markdown with working images"
  default_prompt: "Use $mineru-v3-pdf-parser to parse this PDF and deliver a complete Markdown bundle with working images."
